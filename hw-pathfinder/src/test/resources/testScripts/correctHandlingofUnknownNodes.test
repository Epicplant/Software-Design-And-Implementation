# Tests that if a node doesn't exist within the graph when Dijkstra's algorithm is applied that the node is shown
# as unknown

# Creates a new graph
CreateGraph newGraph

# Attempts to find a path between two non-existent nodes
FindPath newGraph A B

# Adds node A to the graph
AddNode newGraph A

# Attempts to find a path between one non-existent source node and a dest node
FindPath newGraph B A

# Attempts to find a path between a source node and a non-existent dest node
FindPath newGraph A B